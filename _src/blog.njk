---
layout: 'layouts/home.njk'
title: Blog Archive
eleventyNavigation:
  key: Posts
  order: 2
---
<h1>{{ title }}</h1>

<ul class="postlist">
{# Display the year in the blog archive #}
{# from https://chriskirknielsen.com/blog/group-posts-by-year-with-nunjucks-in-eleventy/ #}
{# {% for post in collections.post | reverse %}
	{% set currentYear = '' %}
	{% set postYear = post.date | yearOnly %}
	{% if currentYear != postYear %}
	<h2>{{ postYear }}</h2>
	{% endif %} #}
	<li>
		<p><code>{{ post.date | htmlDateString }} — </code><a href="{{ post.url }}">{{ post.data.title }}</a></p>
	</li>
{# {% endfor %} #}
</ul>


{# 
from https://chriskirknielsen.com/blog/group-posts-by-year-with-nunjucks-in-eleventy/ 

{% set currentYear = '' %}
{% for post in postslist | reverse %}
	{% set postYear = post.date | dateFormat({ format: 'year' }) %}
	{% if currentYear != postYear %}
	<h2>{{ postYear }}</h2>
	{% endif %}

	<p><a href="{{ post.url }}">{{ post.data.title }}</a></p>
	{% set currentYear = postYear %}
{% endfor %}
#}